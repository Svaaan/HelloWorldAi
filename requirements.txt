# What the coordinator and the dashboard need to run.
#
# This file used to be a `pip freeze` of somebody's whole machine: selenium,
# playwright, undetected-chromedriver, Flask, feedparser, beautifulsoup4,
# azure-cosmos and about a hundred others. The project imports seventeen of
# them. Everything else was downloaded, compiled and shipped in every image for
# no reason, and it made the real dependencies impossible to see -- the file
# also did not list pytest, so installing it gave you an environment that could
# not run a single test.
#
# The list below was measured rather than remembered: every import reachable
# from src/backend/coordinator.py and src/app.py, followed through the local
# modules. If you add an import, add it here.
#
# Related files:
#   requirements-node.txt       the smaller set a contributor's node needs
#   requirements-dashboard.txt  the smaller set again for the proxy dashboard
#   requirements-dev.txt        this, plus what it takes to run the tests

# --- web server ---
fastapi==0.115.12
uvicorn==0.34.0
pydantic==2.11.0
# Renders the page templates. It was not listed here for a long time and
# everything worked anyway, because torch happens to depend on it -- so every
# page this project serves was relying on a transitive dependency of PyTorch.
# The measurement that built this list missed it because the import reads
# `from fastapi.templating import Jinja2Templates`, which looks like fastapi.
jinja2==3.1.6

# --- talking to nodes, and to the coordinator from the dashboard ---
httpx==0.28.1
requests==2.32.3

# --- the database. bson arrives with pymongo; motor is the async driver ---
motor==3.7.0
pymongo==4.11.1

# --- session tokens and artifact encryption ---
itsdangerous==2.2.0
cryptography==44.0.2

# --- reading the machine: GPUs, temperature, CPU ---
pynvml==12.0.0
nvidia-ml-py==12.570.86
psutil==7.0.0

# --- training, and the format a finished model leaves in ---
torch==2.6.0
numpy==2.2.3
safetensors==0.5.3

# --- configuration ---
python-dotenv==1.0.1
typing_extensions==4.12.2
